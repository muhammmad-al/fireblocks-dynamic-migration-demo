# === Fireblocks (SECRETS — never commit real values) ===
FIREBLOCKS_API_SECRET="<PEM private key, newlines escaped as \\n>"
FIREBLOCKS_API_KEY_NCW_SIGNER="<UUID of NCW Signer API user>"
FIREBLOCKS_API_KEY_NCW_ADMIN="<UUID of NCW Admin API user>"
FIREBLOCKS_API_BASE_URL="https://sandbox-api.fireblocks.io/"
FIREBLOCKS_WEBHOOK_PUBLIC_KEY="<Fireblocks webhook public key PEM>"

# === Auth (must match frontend Firebase project) ===
JWKS_URI="https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com"
ISSUER="https://securetoken.google.com/<your-firebase-project-id>"
AUDIENCE="<your-firebase-project-id>"

# === Database ===
DB_HOST="localhost"
DB_PORT="3306"
DB_USERNAME="root"
DB_PASSWORD="<your-mysql-password>"
DB_NAME="ncw"

# === Optional ===
CMC_PRO_API_KEY="<CoinMarketCap API key, optional>"
WEBHOOK_SKIP_VERIFY="true"
PORT="3000"
ORIGIN_WEB_SDK="http://localhost:5173"
