# All VITE_ vars are compiled into the browser bundle — NOT secrets
VITE_DYNAMIC_ENVIRONMENT_ID="<from Dynamic dashboard>"
VITE_BACKEND_BASE_URL="http://localhost:3000"
VITE_NCW_SDK_ENV="sandbox"
VITE_AUTOMATE_INITIALIZATION="true"
